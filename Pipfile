# -*- mode: python -*-

[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = "*"
pytest = "*"
numpy = "*"

[dev-packages]
black = "*"

[requires]
python_version = "3.10"

[scripts]
start = "python src/leanStats.py"
test = "pytest tests"
formatcheck = "black --check src tests"
formatdiff = "black --diff src tests"
formatfix =  "black src tests"

[tool]
pytest = {testpaths = ["tests"]}
