[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = "*"
pytest = "*"

[dev-packages]

[requires]
python_version = "3.10"

# run scripts using `pipenv run <name>`
[scripts]
leanStats = "python src/leanStats.py"
test = "pytest tests"


[tool.pytest.ini_options]
pythonpath = "src"
testpaths = ["tests"]
